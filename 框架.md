# 功能

1. 电机能转
2. 能控制电机的位置和转速
3. 能对电机进行电流控制
4. 能在电脑读取电机的状态

# 物料

1. STM32F407VET6
2. 编码器
3. 电机驱动器

# 原理图

## 引脚定义

### 引脚需求

编码器	软件I2C

电机驱动		6根PWM线	3根ADC电流线

VOFA+		串口1

启停线	IO

### 引脚分配

PWM：PE9(TIM1_CH1)、PE11(TIM1_CH2)、PE13(TIM1_CH3)、PB0(TIM1_CH2N)、PB1(TIM1_CH3N)、PA7(TIM1_CH1N)

USART：PA9(USART1_TX)、PA10(USART1_RX)

I2C：PD8、PD9

ADC：PA0(ADC1_IN0)、PA1(ADC1_IN1)、PA2(ADC1_IN2)

启停线：PE1

## 电源

编码器（3.3V，5V）

电机驱动		12V		5V		GND	启停线

# 代码框架

1. 顶层：应用层
   角色：用户的主循环
   职责：实例化FOC_Driver_t，提供底层的硬件函数指针（PWM设置，ADC读取），并周期调用FOC_Driver.Run()
2. 核心层：FOC主控类
   角色：大脑
   属性（成员变量）：
   * hal:包含操作硬件的函数指针
   * pid_id,pid_iq：两个PI控制器实例，分别控制id和iq
   * current_ab,current_dq：储存变换过程中的电流数据
   * voltage_ab,voltage_dq：存储计算出的电压指令
   方法（成员函数指针）：
   * Init:初始化内部PI参数
   * Run：核心算法。按顺序执行：读数据->Clarke->Park->PI运算->反Park->SVPWM->写PWM
3. 组件层：PI控制器类
   * 角色：执行器。
   * 特点: 这是一个可复用的“类”。FOC 主控类里有两个这样的对象（D轴和Q轴各一个）。
   * 封装: 内部包含积分值 integral、比例系数 Kp 等，外部无法随意修改，只能通过 Update 接口进行计算。
4. 基础层：数据结构
   * 角色: 数据载体。
   * 作用: 用于在不同模块（从 ADC 到 FOC，从 FOC 到 PWM）之间传递数据，保证接口统一。
5. 工具层：数学库
   * 角色: 纯计算工具。
   * 特点: 它是“无状态”的（不保存变量），只有输入输出。包含 Clarke、Park、Inverse Park、SVPWM 等算法实现。
6. 底层：硬件抽象层
   * 角色：桥梁
   * 定义: 一组函数指针的集合。
   * 作用: FOC_Driver 不关心你是用 STM32 还是 ESP32，只要你在 Init 时把对应的底层函数（如 TIM_SetCompare）赋值给它，它就能工作。实现了硬件无关
## 速度开环
1. 顶层：应用层
   职责：实例化FOC_Driver_t，提供底层的硬件函数指针（PWM设置，ADC读取），并周期调用FOC_Driver.Run()
2. 核心层：FOC主控
